class Ticket {
  constructor(ticketNumber, eventName, eventDate) {
    this.ticketNumber = ticketNumber;
    this.eventName = eventName;
    this.eventDate = eventDate;
  }
}

module.exports = Ticket;